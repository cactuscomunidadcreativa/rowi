"use client";

export default function SettingsPage() {
  return (
    <div className="text-sm text-gray-600 p-6">
      Configuración global del Hub — límites, políticas, SSO y monetización (pendiente UI).
    </div>
  );
}
