// src/core/startup/ensureTranslations.ts
export async function ensureTranslations() {
  console.log("🌐 ensureTranslations ejecutado — sincronizando idiomas base...");

  // Aquí puedes dejar lógica real o vacía
  // Ejemplo: si quieres asegurar traducciones básicas:
  // const langs = ["es", "en", "pt", "it"];
  // for (const lang of langs) {
  //   console.log(`→ Idioma asegurado: ${lang}`);
  // }

  console.log("✅ Traducciones globales garantizadas (stub).");
}